# name: ncspanda-frontend-staging
# region: "SGP1"

# services:
#   - name: frontend
#     github:
#       repo: personryan/NCSPanda
#       branch: staging
#       deploy_on_push: false  # omit or leave disabled when using GitHub Actions as the driver
#     source_dir: frontend

#     # Choose ONE build approach and complete its required fields:
#     # Option A: Buildpacks (requires correct environment_slug and commands)
#     # environment_slug: <SUPPORTED_BUILDPACK_SLUG>
#     # run_command: <YOUR_RUN_COMMAND>
#     # http_port: <YOUR_HTTP_PORT>

#     # Option B: Dockerfile (set dockerfile_path + any required commands/port)
#     # dockerfile_path: ./Dockerfile
#     # run_command: <YOUR_RUN_COMMAND>
#     # http_port: <YOUR_HTTP_PORT>

# name: ncspanda-backend-staging
# region: "SGP1"

# services:
#   - name: backend
#     github:
#       repo: personryan/NCSPanda
#       branch: staging
#       deploy_on_push: false  # omit or leave disabled when using GitHub Actions as the driver
#     source_dir: backend

    # Choose ONE build approach and complete its required fields:
    # Option A: Buildpacks (requires correct environment_slug and commands)
    # environment_slug: <SUPPORTED_BUILDPACK_SLUG>
    # run_command: <YOUR_RUN_COMMAND>
    # http_port: <YOUR_HTTP_PORT>

    # Option B: Dockerfile (set dockerfile_path + any required commands/port)
    # dockerfile_path: ./Dockerfile
    # run_command: <YOUR_RUN_COMMAND>
    # http_port: <YOUR_HTTP_PORT>