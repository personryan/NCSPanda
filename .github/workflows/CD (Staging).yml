name: Deploy Staging (GHCR -> DO App Platform)

on:
  push:
    branches: [staging]

concurrency:
  group: staging-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  GHCR_OWNER: tck1998
  BACKEND_IMAGE_REPO: ${{ github.event.repository.name }}-backend
  FRONTEND_IMAGE_REPO: ${{ github.event.repository.name }}-frontend

  # Repo variable (Settings -> Secrets and variables -> Actions -> Variables)
  STAGING_URL: ${{ vars.STAGING_URL }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: tck1998
          password: ${{ secrets.STAGING_CONTAINER }}

      # Build locally for scanning (then push only if scan passes)
      - name: Build backend image (local for scan)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: backend/Dockerfile
          load: true
          push: false
          platforms: linux/amd64
          tags: localbuild/backend:scan

      - name: Build frontend image (local for scan)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: frontend/Dockerfile
          load: true
          push: false
          platforms: linux/amd64
          tags: localbuild/frontend:scan

      - name: Scan backend image (fail on critical)
        uses: anchore/scan-action@v7
        with:
          image: localbuild/backend:scan
          fail-build: true
          severity-cutoff: critical

      - name: Scan frontend image (fail on critical)
        uses: anchore/scan-action@v7
        with:
          image: localbuild/frontend:scan
          fail-build: true
          severity-cutoff: critical

      # Push to GHCR (capture digests)
      - name: Build + push backend (GHCR)
        id: push_backend
        uses: docker/build-push-action@v6
        with:
          context: .
          file: backend/Dockerfile
          push: true
          platforms: linux/amd64
          tags: |
            ${{ env.REGISTRY }}/${{ env.GHCR_OWNER }}/${{ env.BACKEND_IMAGE_REPO }}:staging
            ${{ env.REGISTRY }}/${{ env.GHCR_OWNER }}/${{ env.BACKEND_IMAGE_REPO }}:${{ github.sha }}

      - name: Build + push frontend (GHCR)
        id: push_frontend
        uses: docker/build-push-action@v6
        with:
          context: .
          file: frontend/Dockerfile
          push: true
          platforms: linux/amd64
          tags: |
            ${{ env.REGISTRY }}/${{ env.GHCR_OWNER }}/${{ env.FRONTEND_IMAGE_REPO }}:staging
            ${{ env.REGISTRY }}/${{ env.GHCR_OWNER }}/${{ env.FRONTEND_IMAGE_REPO }}:${{ github.sha }}

      # - name: Deploy to DigitalOcean App Platform
      #   uses: digitalocean/app_action/deploy@v2
      #   env:
      #     BACKEND_DIGEST: ${{ steps.push_backend.outputs.digest }}
      #     FRONTEND_DIGEST: ${{ steps.push_frontend.outputs.digest }}
      #   with:
      #     token: ${{ secrets.NCSPANDA_DOSTAGING }}
      #     project_id: ${{ secrets.DO_PROJECT_ID_STAGING }}
      #     app_spec_location: .do/app.yaml
      #     print_build_logs: "true"
      #     print_deploy_logs: "true"

      # - name: OWASP ZAP Baseline (DAST)
      #   uses: zaproxy/action-baseline@v0.15.0
      #   with:
      #     target: ${{ env.STAGING_URL }}
      #     fail_action: true
      #     token: ${{ secrets.GITHUB_TOKEN }}